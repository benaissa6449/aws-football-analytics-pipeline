# IAM Configuration for Football Pipeline
# Note: LabRole is referenced via data source (cannot create new roles in AWS Lab)

# Cette file référence le LabRole existant dans le lab
# Toutes les permissions pour S3, Glue, Athena sont géreées via ce rôle
